void handleCollision(double newX, double newY, double* playerX, double* playerY)
{
    // Check collision with wall at the new position
    if (map[(int)newY][(int)newX] == 0)
    {
        // Update the player's position
        *playerX = newX;
        *playerY = newY;
    }
}
